<p>
<% unless current_user.unlikes?(post) %>

    <% if user_signed_in? && current_user.likes?(post) %>
      <%= link_to "좋아요 취소", post_like_path(post), method: :delete, remote: true %>
    <% else %>
      <%= link_to "좋아요", post_like_path(post), method: :post, remote: true %>
    <% end %>

<% end %>


  <% post.likes.each do |like| %>
    <%= image_tag like.user.avatar_url, width: 20 %>
  <% end %>


</p>
<p>좋아요 갯수 <%= post.likes.count %></p>